- file:
    state: "directory"
    path: "{{ item }}"
    owner: bb
    mode: 0755
    recurse: yes
  with_items:
    - "[[ dirs.role_data ]]"
    - "[[ dirs.role_data ]]/dfs"
    - "[[ dirs.role_data ]]/dfs/nn"
